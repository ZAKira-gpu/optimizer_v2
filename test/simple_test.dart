import 'package:flutter_test/flutter_test.dart';
import 'package:optimizer/app.dart';

void main() {
  test('App can be instantiated', () {
    const app = OptimizerApp();
    expect(app, isNotNull);
  });
}
